# Setup - Taskfile

## Como usar

Execute uma das tarefas usando o binário local `task`:

```bash
# Executar todas as tarefas
./task all

# Executar tarefas base
./task base
```

## Tarefas disponíveis

- `all` - Executa todas as tarefas
- `base` - Executa tarefas base
